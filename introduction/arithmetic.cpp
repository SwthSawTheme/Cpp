#include <iostream>

int main(){
    int students = 20;

    // students = students + 1;
    // students += 1;
    // students++;
    // students *= 2;
    // students--;
    // students -= 5;
    // students /= 2;
    // students %= 2;

    // precedencia aritmética
    // parenteses
    // multiplicação e divisao
    // adição e subtração

    int aritmetic = 2 + 2 / 5 * 8 + 5;

    std::cout << students << std::endl;
    std::cout << aritmetic << std::endl;
}